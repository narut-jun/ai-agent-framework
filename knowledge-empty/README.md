# 📚 Knowledge Base — Empty Template

> **โฟลเดอร์นี้คือ knowledge เปล่า สำหรับเริ่มต้นใหม่**  
> วิธีใช้: Rename `knowledge/` เก่า → Rename `knowledge-empty/` เป็น `knowledge/`  
> เวอร์ชัน: **v0.0.0** (ว่าง)

---

## โครงสร้าง

```
knowledge-empty/
├── README.md              ← คุณอยู่ที่นี่
├── global-rules/
│   ├── owner_identity.md  ← [ใส่ข้อมูลเจ้าของ]
│   ├── ai_collaboration.md ← [ใส่กฎการทำงานกับ AI]
│   └── cost_policy.md     ← [ใส่นโยบายค่าใช้จ่าย]
├── patterns/              ← (ว่าง — รอ extract)
└── skills/                ← (ว่าง — รอ extract)
```

## วิธีเริ่มต้น

1. กรอกข้อมูลใน `global-rules/owner_identity.md`
2. ปรับ `ai_collaboration.md` ตามสไตล์การทำงาน
3. ปรับ `cost_policy.md` ตามนโยบาย
4. เริ่มสะสม patterns/ และ skills/ จากการทำงานจริง
